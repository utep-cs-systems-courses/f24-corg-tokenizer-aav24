# creating main makefile

# object files
OBJS= uimain.o src/tokenizer.o

CFLAGS= -g -O3

all: main

main: $(OBJS)
	cc -o main $(CFLAGS) $(OBJS)

$(OBJS): src/tokenizer.h

# cleans object files
clean:
	rm -f *.o src/*.o main


run:main
	./main
